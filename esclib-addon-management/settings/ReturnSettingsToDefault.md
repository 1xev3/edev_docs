# ReturnSettingsToDefault

## Usage example
```lua
addon:ReturnSettingsToDefault()
```

## Notes
Resets all settings to their default values and deletes settings files. Triggers settings changed hook.
